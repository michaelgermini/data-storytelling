import { ReactNode } from 'react'\n\nexport function SidePanel({ title, children }: { title: string, children: ReactNode }) {\n  return (\n    <aside style={{ position: 'fixed', right: 0, top: 0, bottom: 0, width: 360, background: '#0f131a', borderLeft: '1px solid #1f2a35', padding: 16, overflow: 'auto' }}>\n      <h2 style={{ marginTop: 0 }}>{title}</h2>\n      <div>{children}</div>\n    </aside>\n  )\n}\n
